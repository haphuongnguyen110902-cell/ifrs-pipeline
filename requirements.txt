arelle-release==2.44.4
pandas==3.0.5
# numpy and requests are imported directly by the scripts (they only arrived
# transitively via pandas / yfinance) - declared so a change in those packages'
# dependencies cannot silently remove them. pypdf is deliberately NOT listed: it
# is only used by a live-database-only test, lazily imported.
# RANGES, not exact pins, on purpose: CI runs Python 3.11 while numpy 2.5.x needs
# >= 3.12 (an exact numpy==2.5.2 pin, chosen from a 3.13 venv, broke CI's install),
# and pandas already decides which numpy each Python gets.
numpy>=2.0,<3
requests>=2.31,<3
SQLAlchemy==2.0.52
psycopg2-binary==2.9.12
python-dotenv==1.2.3
PyYAML==6.0.3
pytest==9.1.1
streamlit==1.63.0
yfinance==1.7.0
openpyxl==3.1.5
reportlab==5.0.1
