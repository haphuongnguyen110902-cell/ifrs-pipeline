# Requirements for app.py ONLY (the Streamlit dashboard), separate from
# the full requirements.txt used by the data pipeline scripts.
#
# WHY THIS FILE EXISTS: requirements.txt includes arelle-release, which
# app.py never imports (only 09_batch_load.py / load_historical.py need
# it for XBRL parsing) - arelle is a heavy dependency with many
# transitive requirements, risking a slow or failed Streamlit Cloud
# build for no benefit, since the dashboard only ever reads already-
# parsed data from the database.
#
# When deploying on share.streamlit.io: Advanced settings -> Python
# requirements file -> point this file instead of requirements.txt.

pandas>=2.2.0
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
streamlit>=1.38.0
